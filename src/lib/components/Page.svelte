<main class="page">
	<slot />
</main>

<style>
	.page {
		max-width: var(--container-max-width);
		margin-left: auto;
		margin-right: auto;
		margin-bottom: var(--margin-xl);
		padding-left: 32px;
		padding-right: 32px;
	}
</style>
